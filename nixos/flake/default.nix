# NixOS-specific configuration module
{ inputs, ... }:
{
  # This is where you'll define nixosConfigurations
  # Example:
  # flake.nixosConfigurations = {
  #   myhost = inputs.nixpkgs.lib.nixosSystem {
  #     modules = [ ./hosts/myhost ];
  #   };
  # };
}
